name: Keep Backend Alive
on:
  schedule:
    - cron: '*/14 * * * *'  # Cada 14 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping endpoint
        run: curl -X GET https://go-auth-google.onrender.com/health